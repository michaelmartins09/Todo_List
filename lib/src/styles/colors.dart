
import 'package:flutter/material.dart';

const defaultColor = Colors.white;
const defaultBackground = Colors.deepPurple;
const defaultBackgrounDark = Colors.deepPurple;
const LinearGradient defaultGradiend = LinearGradient(colors: [Colors.deepPurple, Colors.deepPurpleAccent]
);